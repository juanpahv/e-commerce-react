import './css/footer.css'

function Footer() {
  // const { cart } = useCart()

  return (
    <footer className='footer'>
      {/* {JSON.stringify(cart, null, 2)} */}
    </footer>
  )
}

export default Footer